# Backend public base URL (used in messages to drivers)
BASE_URL=http://127.0.0.1:8001

# Telegram bot username (without @)
BOT_USERNAME=SeuBotUsername

# Telegram bot token
BOT_TOKEN=coloque_o_token_aqui

# Database URL (Postgres recomendado em produção)
# Exemplo Supabase:
# DATABASE_URL=postgresql://postgres:<SENHA>@<HOST>:5432/postgres
# Em desenvolvimento, deixe vazio para usar SQLite local.
# DATABASE_URL=

# As fotos não são salvas em disco; são mantidas no Telegram (armazenamos apenas file_id).
